import * as React from "react";

export default class RecipeImage extends React.PureComponent {

    // Next Previous button
    // Thumbs up / Thumbs down
    // Image

  render() {
    return (
      <div>
          
      </div>
    );
  }
}

