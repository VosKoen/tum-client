

const initialState = {
    title: null,
    description: null,
    ingredients: [],
    steps: [],
}

export default (state = initialState, action = []) => {
  switch (action.type) {

    default:
      return state;
  }
};
